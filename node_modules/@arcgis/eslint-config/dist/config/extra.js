// src/config/extra.ts
var extra_default = [
  {
    files: ["**/*.ts", "**/*.tsx"],
    rules: {
      // See https://devtopia.esri.com/WebGIS/arcgis-web-components/discussions/446#discussioncomment-3166
      "@typescript-eslint/no-unsafe-return": "warn",
      /*
       * Lot's of false positives on pure functions that don't use
       * "this". For example:
       * const { whenOnce } = await importCoreReactiveUtils();
       */
      "@typescript-eslint/unbound-method": "warn",
      /*
       * This rule can have many positives (i.e abstract methods), or fire on code
       * that you can't refactor without breaking changes
       */
      "class-methods-use-this": "off",
      "@typescript-eslint/class-methods-use-this": [
        "warn",
        {
          ignoreOverrideMethods: true,
          ignoreClassesThatImplementAnInterface: "public-fields"
        }
      ],
      /**
       * False positive when constructor exists to improve typing/narrow down the
       * type
       */
      "@typescript-eslint/no-useless-constructor": "warn"
    }
  }
];
export {
  extra_default as default
};
